# godot-mcp-toolkit
A Godot 4.x editor plugin that exposes the editor to AI coding assistants via the Model Context Protocol (MCP). Pair   it with the godot-mcp-server npm package and Claude Code (or any MCP-compatible assistant) can create scenes,   manipulate nodes, edit scripts, and launch the game directly from chat — no manual copying between the AI and Godot.
